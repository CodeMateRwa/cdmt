# Fonts

Place `Horizon.otf` (by Bitfoundry) in this folder.

The file is referenced as:
  src/fonts/Horizon.otf

It is loaded via `@font-face` in `src/index.css`.
Fallback chain: Orbitron (Google Fonts CDN) → sans-serif.
